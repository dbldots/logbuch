html
  body
    ion-view(view-title='Logbuch Einstellungen', cache-view="false")
      ion-content(has-header='true', padding='true')
        ion-list
          label.item.item-input.item-stacked-label
            span.input-label Name (für PDF Export)
            input(type='text', ng-model='settings.name', placeholder='Ihr Name')

          ion-toggle(ng-model='settings.debug') Debug Modus (Aktiviere Debug Log)

          button.button.button-full.button-positive(ng-click='save()') Speichern
          button.button.button-full.button(ng-click='cancel()') Abbrechen
